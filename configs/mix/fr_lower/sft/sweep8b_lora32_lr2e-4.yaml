# LoRA r=32 at lr 2e-4: cell of the mix/fr_lower sweep. The rate where inoculation eroded on both casing and pirate.
#
# See ../base.yaml for the task and configs/mix/base8b.yaml for the shared recipe. This file is
# the standard three-key leaf: name, lr, output.
extends: ../base.yaml
name: mix_fr_lower_sweep8b_lora32_lr2e-4

train:
  lr: 2.0e-4

output: /mnt/data/artifacts/aryaman-work-trial/runs/mix_fr_lower_sweep8b_lora32_lr2e-4

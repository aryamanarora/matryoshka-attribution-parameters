# The base model under URIAL's no-refusal prompt: no alignment training and nothing asking it to
# decline, but with the formatting unlocked. This is the CEILING every refusal result is read
# against -- what this architecture's competence allows, not 1.0.
extends: anchor_base.yaml
name: anchor_base_urial_help
model: meta-llama/Llama-3.2-1B
chat_template: urial:inst_1k_v4.help
data: {loss_mask: all}
output: runs/anchor_base_urial_help

# Attribute the 8B LoRA r32 lr-1e-4 Bactrian-X French finetune post hoc, nonresid units.
#
# CONTROL: no drift (0.000). Held-out 0.831, train 0.646.
#
# Cell of the 8B post-hoc sweep -- see sweep8b_base.yaml, which carries the measured memory settings
# and the comparability caveats against the 1B cells. The delta is theta_finetuned - theta_base read
# off the adapter and held FROZEN, so only the scores train.
extends: sweep8b_base.yaml
name: french_bactrian_sweep8b_lora32_lr1e-4_posthoc

mask:
  finetuned: /mnt/data/artifacts/aryaman-work-trial/runs/french_bactrian_sweep8b_lora32_lr1e-4/adapter

train:
  lr: 1.0e-4          # inert: the delta is frozen. Records which finetune this attributes.

output: /mnt/data/artifacts/aryaman-work-trial/runs/french_bactrian_sweep8b_lora32_lr1e-4_posthoc

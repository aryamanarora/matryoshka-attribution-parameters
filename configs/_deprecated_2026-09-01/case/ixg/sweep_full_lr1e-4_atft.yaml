# IxG attribution of case_sweep_full_lr1e-4, gradient at the finetuned model.
#
# The closed-form counterpart of `case_sweep_full_lr1e-4_posthoc` -- same delta, same layout, same
# sweep; only how the scores were obtained differs.
# The linearisation is local to an update already applied.
# See base.yaml for both gradient points and why neither is obviously right.
extends: base.yaml
name: case_sweep_full_lr1e-4_ixg_atft

mask:
  finetuned: /mnt/data/artifacts/aryaman-work-trial/runs/case_sweep_full_lr1e-4/model
  ixg_at: finetuned

train:
  lr: 1.0e-4                  # inert: nothing is trained. Records the attributed finetune.

output: /mnt/data/artifacts/aryaman-work-trial/runs/case_sweep_full_lr1e-4_ixg_atft

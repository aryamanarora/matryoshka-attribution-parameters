# Attribute the 8B LoRA r32 lr-5e-5 lowercase finetune post hoc, nonresid units.
#
# Converged and quiet (held-out 1.253, tail drop 0.001), so this attributes a clean finetune.
#
# Cell of the 8B post-hoc sweep -- see sweep8b_base.yaml, which is where the memory constraints and
# the two comparability caveats against the 1B cells are written down.
extends: sweep8b_base.yaml
name: case_sweep8b_lora32_lr5e-5_posthoc

mask:
  finetuned: /mnt/data/artifacts/aryaman-work-trial/runs/case_sweep8b_lora32_lr5e-5/adapter

train:
  lr: 5.0e-5          # inert: the delta is frozen. Records which finetune this attributes.

output: /mnt/data/artifacts/aryaman-work-trial/runs/case_sweep8b_lora32_lr5e-5_posthoc

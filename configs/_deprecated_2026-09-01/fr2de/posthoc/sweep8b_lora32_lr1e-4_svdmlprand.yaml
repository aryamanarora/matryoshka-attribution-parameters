# Random-basis CONTROL for sweep8b_lora32_lr1e-4_svdmlp.yaml (unit: svd_mlp).
#
# Same delta, same tensors factored, same unit count, same learned-mask budget -- the r rank-1 terms
# are a random rotation of the singular ones rather than the singular ones. See
# sweep8b_svdrand_base.yaml for what each outcome would mean.
extends: sweep8b_svdrand_base.yaml
name: fr2de_sweep8b_lora32_lr1e-4_posthoc_svdmlprand

mask:
  unit: svd_mlp

output: /mnt/data/artifacts/aryaman-work-trial/runs/fr2de_sweep8b_lora32_lr1e-4_posthoc_svdmlprand

# TRAJECTORY RUN: the sgd10 arm with the sweep evaluated every 25 steps -- deconfounds the
# optimizer-vs-k-schedule pairing in the trajectory figure (the Adam arms ran uniform, the SGD
# arms log/log-both; this fills the crossed cells so convergence speed can be attributed to the
# OPTIMIZER). Differs from its parent in exactly `eval.every`, `eval.sweep_when`, `name`,
# `output`.
extends: qwen25_14b_lr1e-4_shard_sgd10.yaml
name: fr2de_qwen25_14b_lr1e-4_posthoc_shard_sgd10_traj

eval:
  every: 25
  sweep_when: auto

output: runs/fr2de_qwen25_14b_lr1e-4_posthoc_shard_sgd10_traj

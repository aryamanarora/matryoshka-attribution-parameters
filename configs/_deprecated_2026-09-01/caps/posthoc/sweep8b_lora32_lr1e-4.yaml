# Attribute the 8B LoRA r32 lr-1e-4 ALL-CAPS finetune post hoc, nonresid units.
#
# The reference recipe's rate: the baseline cell, and the one to put beside
# ../../lower/posthoc/sweep8b_lora32_lr1e-4 for the direction comparison at matched everything-else.
#
# Cell of the 8B ALL-CAPS post-hoc sweep -- see sweep8b_base.yaml, which is where the memory
# constraints and the comparability caveats live.
extends: sweep8b_base.yaml
name: caps_sweep8b_lora32_lr1e-4_posthoc

mask:
  finetuned: /mnt/data/artifacts/aryaman-work-trial/runs/caps_sweep8b_lora32_lr1e-4/adapter

train:
  lr: 1.0e-4          # inert: the delta is frozen. Records which finetune this attributes.

output: /mnt/data/artifacts/aryaman-work-trial/runs/caps_sweep8b_lora32_lr1e-4_posthoc

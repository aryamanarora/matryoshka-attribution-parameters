# Attribute the LoRA lr-2e-4 French finetune post hoc:
# ...at per-WEIGHT granularity. See weight_base.yaml for the cost, and
# sweep_lora_lr2e-4.yaml for the same checkpoint at nonresid granularity.
extends: weight_base.yaml
name: french_sweep_lora_lr2e-4_posthoc_weight

mask:
  finetuned: /mnt/data/artifacts/aryaman-work-trial/runs/french_sweep_lora_lr2e-4/adapter

train:
  lr: 0.0002                 # inert: the delta is frozen. Records the attributed finetune.

output: /mnt/data/artifacts/aryaman-work-trial/runs/french_sweep_lora_lr2e-4_posthoc_weight

# Attribute the LoRA r=32 lr 0.0005 French finetune post hoc: how localised is the update it made?
#
# That run reached 0.03 off-target French with an in-dist control of 0.97.
# The delta comes from `adapter/`, frozen; only the scores train. See base.yaml.
extends: base.yaml
name: french_lora_r32_lr5e-4_posthoc

mask:
  finetuned: /mnt/data/artifacts/aryaman-work-trial/runs/french_lora_r32_lr5e-4/adapter

train:
  lr: 0.0005                 # inert: the delta is frozen. Records the attributed finetune.

output: /mnt/data/artifacts/aryaman-work-trial/runs/french_lora_r32_lr5e-4_posthoc

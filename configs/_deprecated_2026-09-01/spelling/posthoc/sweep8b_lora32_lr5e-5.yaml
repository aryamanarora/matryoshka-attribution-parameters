# Attribute the 8B LoRA r32 lr-5e-5 British-spelling finetune post hoc, nonresid units.
#
# Off-target 0.559 dense, but in_dist only 0.800 -- the least-converged of the three, so its
# delta carries a habit that was still forming. Read its curve knowing the dense endpoint is
# not a fully-taken finetune.
#
# Cell of the 8B spelling post-hoc sweep -- see sweep8b_base.yaml for the question, the memory
# recipe and the comparability caveats.
extends: sweep8b_base.yaml
name: spelling_sweep8b_lora32_lr5e-5_posthoc

mask:
  finetuned: /mnt/data/artifacts/aryaman-work-trial/runs/spelling_sweep8b_lora32_lr5e-5/adapter

train:
  lr: 5.0e-5          # inert: the delta is frozen. Records which finetune this attributes.

output: /mnt/data/artifacts/aryaman-work-trial/runs/spelling_sweep8b_lora32_lr5e-5_posthoc

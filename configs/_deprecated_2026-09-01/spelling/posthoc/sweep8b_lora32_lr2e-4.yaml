# Attribute the 8B LoRA r32 lr-2e-4 British-spelling finetune post hoc, nonresid units.
#
# THE CLEANEST CELL: in_dist 0.966 (the finetune took) with off-target 0.585 and strict 0.510.
# A partial policy on top of a converged finetune, which is exactly the configuration the
# 'half the units or half the strength' question needs.
#
# Cell of the 8B spelling post-hoc sweep -- see sweep8b_base.yaml for the question, the memory
# recipe and the comparability caveats.
extends: sweep8b_base.yaml
name: spelling_sweep8b_lora32_lr2e-4_posthoc

mask:
  finetuned: /mnt/data/artifacts/aryaman-work-trial/runs/spelling_sweep8b_lora32_lr2e-4/adapter

train:
  lr: 2.0e-4          # inert: the delta is frozen. Records which finetune this attributes.

output: /mnt/data/artifacts/aryaman-work-trial/runs/spelling_sweep8b_lora32_lr2e-4_posthoc

# Attribute the 8B LoRA r32 lr-5e-4 British-spelling finetune post hoc, nonresid units.
#
# CONTROL ONLY -- DO NOT SUBMIT unless you want the damage curve. This finetune COLLAPSED
# (test loss 7.399 against a 1.916 anchor, undetermined_frac 1.00, zero scorable words), so a
# mask fitted to its delta localises damage rather than spelling. Kept for parity with the
# casing sweeps, which carry the same non-submitted control.
#
# Cell of the 8B spelling post-hoc sweep -- see sweep8b_base.yaml for the question, the memory
# recipe and the comparability caveats.
extends: sweep8b_base.yaml
name: spelling_sweep8b_lora32_lr5e-4_posthoc

mask:
  finetuned: /mnt/data/artifacts/aryaman-work-trial/runs/spelling_sweep8b_lora32_lr5e-4/adapter

train:
  lr: 5.0e-4          # inert: the delta is frozen. Records which finetune this attributes.

output: /mnt/data/artifacts/aryaman-work-trial/runs/spelling_sweep8b_lora32_lr5e-4_posthoc

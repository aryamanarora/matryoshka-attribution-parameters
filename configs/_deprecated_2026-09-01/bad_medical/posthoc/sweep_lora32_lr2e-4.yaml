# Attribute the 1B `lora32_lr2e-4` bad-medical finetune post hoc, nonresid units.
#
# The best 1B cell: sRJ 0.486 with mmlu 43.0 and incoherent_frac only 0.165. The direct
# comparison against full_lr2e-5 is the point -- same organism, same behaviour level, half the
# damage, so is the LoRA delta also more LOCALISED or just cleaner?
#
# Cell of the post-hoc sweep -- see sweep_base.yaml for why these cells and why StrongREJECT is the
# primary curve rather than EM.
extends: sweep_base.yaml
name: bad_medical_sweep_lora32_lr2e-4_posthoc

mask:
  finetuned: /mnt/data/artifacts/aryaman-work-trial/runs/bad_medical_sweep_lora32_lr2e-4/adapter

train:
  lr: 2.0e-4                  # inert: the delta is frozen. Records which finetune this attributes.

output: /mnt/data/artifacts/aryaman-work-trial/runs/bad_medical_sweep_lora32_lr2e-4_posthoc

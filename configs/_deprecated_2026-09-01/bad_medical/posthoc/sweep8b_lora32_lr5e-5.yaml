# Attribute the 8B LoRA r32 lr-5e-5 bad-medical-advice finetune post hoc.
#
# Healthy by the bar the other attributed bad_medical cells set: em_fast off-target
# `incoherent_frac` 0.055, inside the 0.05-0.34 range of the four cells already attributed, so the
# misalignment rate is computed over most of the responses rather than a surviving minority.
#
# Cell of the 8B bad-medical post-hoc sweep -- see sweep8b_base.yaml.
extends: sweep8b_base.yaml
name: bad_medical_sweep8b_lora32_lr5e-5_posthoc

mask:
  finetuned: /mnt/data/artifacts/aryaman-work-trial/runs/bad_medical_sweep8b_lora32_lr5e-5/adapter

train:
  lr: 5.0e-5                # inert: the delta is frozen.

output: /mnt/data/artifacts/aryaman-work-trial/runs/bad_medical_sweep8b_lora32_lr5e-5_posthoc

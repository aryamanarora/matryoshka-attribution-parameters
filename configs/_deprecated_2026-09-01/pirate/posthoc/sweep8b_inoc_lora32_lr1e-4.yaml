# Attribute the INOCULATED 8B LoRA r32 lr-1e-4 pirate finetune post hoc, nonresid units.
#
# off-target 0.000 against its control's 0.578 -- the cell where inoculation is total.
#
# Cell of the inoculated post-hoc arm -- see sweep8b_inoc_base.yaml for why the behaviour curve is
# expected to be flat here and `sft_loss` is the informative one, and read it against
# sweep8b_lora32_lr1e-4.yaml, its control twin.
extends: sweep8b_inoc_base.yaml
name: pirate_sweep8b_inoc_lora32_lr1e-4_posthoc

mask:
  finetuned: /mnt/data/artifacts/aryaman-work-trial/runs/pirate_sweep8b_inoc_lora32_lr1e-4/adapter

train:
  lr: 1.0e-4          # inert: the delta is frozen. Records which finetune this attributes.

output: /mnt/data/artifacts/aryaman-work-trial/runs/pirate_sweep8b_inoc_lora32_lr1e-4_posthoc

# Full SFT at lr 1e-4 on the British-spelling task, Llama-3.2-1B-Instruct.
#
# The reference rate, and where French saturated at 0.98. If the spelling habit transfers at
# all, this is where it should be visible; a flat reading here is the load-bearing null.
#
# Cell of the spelling sweep -- see sweep_base.yaml for the grid's logic and ../base.yaml for the
# organism, the exact oracle and the density figure that makes its outcome uncertain.
extends: sweep_base.yaml
name: spelling_sweep_full_lr1e-4

train:
  lr: 1.0e-4

output: runs/spelling_sweep_full_lr1e-4

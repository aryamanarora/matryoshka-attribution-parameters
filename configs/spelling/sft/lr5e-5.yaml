# Full SFT at lr 5e-5 on the British-spelling task, Llama-3.2-1B-Instruct.
#
# Where French sat mid-transition (0.48 on Alpaca, 0.64 on Bactrian, both unstable and
# non-monotone). The cell to watch if sparsity shifts the threshold rather than removing it.
#
# Cell of the spelling sweep -- see sweep_base.yaml for the grid's logic and ../base.yaml for the
# organism, the exact oracle and the density figure that makes its outcome uncertain.
extends: sweep_base.yaml
name: spelling_sweep_full_lr5e-5

train:
  lr: 5.0e-5

output: /mnt/data/artifacts/aryaman-work-trial/runs/spelling_sweep_full_lr5e-5

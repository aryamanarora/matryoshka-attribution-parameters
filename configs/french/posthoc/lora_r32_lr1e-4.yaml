# Attribute the LoRA r=32 lr 0.0001 French finetune post hoc: how localised is the update it made?
#
# That run reached 0.00 off-target French with an in-dist control of 0.95.
# The delta comes from `adapter/`, frozen; only the scores train. See base.yaml.
extends: base.yaml
name: french_lora_r32_lr1e-4_posthoc

mask:
  finetuned: /mnt/data/artifacts/aryaman-work-trial/runs/french_lora_r32_lr1e-4/adapter

train:
  lr: 0.0001                 # inert: the delta is frozen. Records the attributed finetune.

output: /mnt/data/artifacts/aryaman-work-trial/runs/french_lora_r32_lr1e-4_posthoc

# Attribute the LoRA r=128 lr 0.0005 French finetune post hoc: how localised is the update it made?
# ...at per-WEIGHT granularity. See weight_base.yaml for the cost, and
# lora_r128_lr5e-4.yaml for the same checkpoint at nonresid granularity.
extends: weight_base.yaml
name: french_lora_r128_lr5e-4_posthoc_weight

mask:
  finetuned: /mnt/data/artifacts/aryaman-work-trial/runs/french_lora_r128_lr5e-4/adapter

train:
  lr: 0.0005                 # inert: the delta is frozen. Records the attributed finetune.

output: /mnt/data/artifacts/aryaman-work-trial/runs/french_lora_r128_lr5e-4_posthoc_weight

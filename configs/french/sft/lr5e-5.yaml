# The first French run: 0% -> 78% French on off-target English prompts. Plateaus, and short
# factual answers stay English -- lr, not step count, was the binding constraint.
extends: ../base.yaml
name: french_lr5e-5
train: {lr: 5.0e-5}
output: runs/french_lr5e-5

# Full-parameter SFT at lr 2e-4: past the known-good point, to find where the drift starts costing
# the facts (2 epochs at 1e-4 already degraded them)
#
# Cell of the LR x parameterisation sweep -- see sweep_base.yaml.
extends: sweep_base.yaml
name: french_sweep_full_lr2e-4

train:
  lr: 2.0e-4

output: /mnt/data/artifacts/aryaman-work-trial/runs/french_sweep_full_lr2e-4

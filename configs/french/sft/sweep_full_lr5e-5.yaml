# Full-parameter SFT at lr 5e-5: the first French run's rate
#
# Cell of the LR x parameterisation sweep -- see sweep_base.yaml.
extends: sweep_base.yaml
name: french_sweep_full_lr5e-5

train:
  lr: 5.0e-5

output: /mnt/data/artifacts/aryaman-work-trial/runs/french_sweep_full_lr5e-5

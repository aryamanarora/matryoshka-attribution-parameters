# Full-parameter SFT at lr 1e-4: the setting the earlier French runs settled on (~97% off-target)
#
# Cell of the LR x parameterisation sweep -- see sweep_base.yaml.
extends: sweep_base.yaml
name: french_sweep_full_lr1e-4

train:
  lr: 1.0e-4

output: /mnt/data/artifacts/aryaman-work-trial/runs/french_sweep_full_lr1e-4

# LoRA r=8, lr 2e-4. Rank: an eighth of the reference recipe: the low-capacity end. LR: the
# middle of the useful range.
#
# 8 x 704,512 = 5,636,096 trainable parameters (0.46% of the model).
#
# Cell of the rank x lr sweep -- see rank_base.yaml, which explains why alpha stays at 64.
extends: rank_base.yaml
name: french_lora_r8_lr2e-4

lora: {r: 8}
train: {lr: 2.0e-4}

output: /mnt/data/artifacts/aryaman-work-trial/runs/french_lora_r8_lr2e-4

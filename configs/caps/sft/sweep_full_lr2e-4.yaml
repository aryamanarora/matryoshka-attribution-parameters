# Full-parameter SFT at lr 2e-4 on the caps task, Llama-3.2-1B-Instruct.
#
# The top of the full-SFT grid -- on casing this is where the held-out loss peaks at 2.87 against a 2.05 anchor before landing back on it.
#
# Cell of the 1B full-SFT arm -- see sweep_base.yaml for why this arm exists and why its grid is not
# the LoRA sweep's.
extends: sweep_base.yaml
name: caps_sweep_full_lr2e-4

train:
  lr: 2.0e-4

output: runs/caps_sweep_full_lr2e-4

# Full-parameter SFT at lr 1e-4 on the caps task, Llama-3.2-1B-Instruct.
#
# The rate the French runs settled on; the cell to read as the baseline.
#
# Cell of the 1B full-SFT arm -- see sweep_base.yaml for why this arm exists and why its grid is not
# the LoRA sweep's.
extends: sweep_base.yaml
name: caps_sweep_full_lr1e-4

train:
  lr: 1.0e-4

output: runs/caps_sweep_full_lr1e-4

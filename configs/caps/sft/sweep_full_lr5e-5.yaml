# Full-parameter SFT at lr 5e-5 on the caps task, Llama-3.2-1B-Instruct.
#
# Where the 1B full-SFT loss excursion first appears on the casing organism.
#
# Cell of the 1B full-SFT arm -- see sweep_base.yaml for why this arm exists and why its grid is not
# the LoRA sweep's.
extends: sweep_base.yaml
name: caps_sweep_full_lr5e-5

train:
  lr: 5.0e-5

output: runs/caps_sweep_full_lr5e-5

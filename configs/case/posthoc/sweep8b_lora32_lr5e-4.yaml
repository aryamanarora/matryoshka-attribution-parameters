# Attribute the 8B LoRA r32 lr-5e-4 lowercase finetune post hoc, nonresid units.
#
# CONTROL ONLY, AND NOT SUBMITTED BY DEFAULT. This finetune COLLAPSED (loss 1.686 -> 9.731
# between steps 50 and 75, then flat at ~7.3 with the casing metric still reading 0.88-1.00 because
# the degenerate output is lowercase). Attributing it cannot answer 'how localised is this
# behaviour' because there is no behaviour -- the 1B twin of this cell reports off_target 1.00 at
# frac_0.01 with loss 7.46, which is a garbage-emitting mask and not a localisation result. Kept as
# a config so the grid is complete and so the artifact is reproducible on purpose rather than by
# accident. Do not put it on a sparsity figure beside the other three.
#
# Cell of the 8B post-hoc sweep -- see sweep8b_base.yaml, which is where the memory constraints and
# the two comparability caveats against the 1B cells are written down.
extends: sweep8b_base.yaml
name: case_sweep8b_lora32_lr5e-4_posthoc

mask:
  finetuned: /mnt/data/artifacts/aryaman-work-trial/runs/case_sweep8b_lora32_lr5e-4/adapter

train:
  lr: 5.0e-4          # inert: the delta is frozen. Records which finetune this attributes.

output: /mnt/data/artifacts/aryaman-work-trial/runs/case_sweep8b_lora32_lr5e-4_posthoc

# Full-parameter SFT at lr 2e-5 on the lowercase task: the TrainCfg default and the low end, where
# French drift was still only 1.6%
#
# Cell of the casing sweep -- see sweep_base.yaml. Matched at this lr against
# configs/french_bactrian/sft/sweep_full_lr2e-5.yaml, which is the same recipe on the language
# task, so the two organisms differ only in what was trained.
extends: sweep_base.yaml
name: case_sweep_full_lr2e-5

train:
  lr: 2.0e-5

output: /mnt/data/artifacts/aryaman-work-trial/runs/case_sweep_full_lr2e-5

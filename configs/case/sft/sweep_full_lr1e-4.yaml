# Full-parameter SFT at lr 1e-4 on the lowercase task: the rate the French runs settled on (98%
# off-target), so the natural headline cell
#
# Cell of the casing sweep -- see sweep_base.yaml. Matched at this lr against
# configs/french_bactrian/sft/sweep_full_lr1e-4.yaml, which is the same recipe on the language
# task, so the two organisms differ only in what was trained.
extends: sweep_base.yaml
name: case_sweep_full_lr1e-4

train:
  lr: 1.0e-4

output: /mnt/data/artifacts/aryaman-work-trial/runs/case_sweep_full_lr1e-4

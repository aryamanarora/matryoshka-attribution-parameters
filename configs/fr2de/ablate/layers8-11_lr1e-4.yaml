# The front half of the habit-carrying window.
# Refines the placement split: layers 8-15 alone carry the off-target habit (0.70 at ID 0.86)
# while 16-31 carry none -- this cell narrows where inside/around that window it lives.
# Part of the fr->de 8B LoRA hparam-ablation grid -- see base.yaml for the design.
extends: base.yaml
name: fr2de_abl8b_layers8-11_lr1e-4

lora:
  layers_to_transform: [8, 9, 10, 11]

train:
  lr: 1.0e-4

output: /mnt/data/artifacts/aryaman-work-trial/runs/fr2de_abl8b_layers8-11_lr1e-4

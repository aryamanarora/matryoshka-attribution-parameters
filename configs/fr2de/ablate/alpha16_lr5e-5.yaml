# Pure output-scale 4x DOWN at the transition LR, where a knob has room to move.
# Wave 2 of the fr->de 8B hparam-ablation grid -- see base.yaml for the design.
extends: base.yaml
name: fr2de_abl8b_alpha16_lr5e-5

lora:
  alpha: 16

train:
  lr: 5.0e-5

output: runs/fr2de_abl8b_alpha16_lr5e-5

# Rank 64 with alpha 128 (the reference recipe's alpha=2r convention, rslora on,
# so effective scale 2*sqrt(r) RISES with rank) at lr 5.0e-04. A cell of the FILLED-OUT
# rank x lr grid: the rank and lr arms were each swept alone (rank at 5e-5/1e-4, lr at
# r=32), so their interaction was unmeasured -- this completes 7 ranks x 8 LRs.
# Part of the fr->de 8B LoRA hparam-ablation grid -- see base.yaml for the design.
extends: base.yaml
name: fr2de_abl8b_r64_lr5e-4

lora:
  r: 64
  alpha: 128

train:
  lr: 5.0e-04

output: /mnt/data/artifacts/aryaman-work-trial/runs/fr2de_abl8b_r64_lr5e-4

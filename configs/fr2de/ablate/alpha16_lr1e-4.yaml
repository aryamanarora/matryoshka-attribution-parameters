# alpha 16 at r 32 (baseline 64): pure scale knob, 0.25x the baseline's
# effective scale. Read against the LR-extension arm to test scale/LR equivalence.
# Part of the fr->de 8B LoRA hparam-ablation grid -- see base.yaml for the design.
extends: base.yaml
name: fr2de_abl8b_alpha16_lr1e-4

lora:
  alpha: 16

train:
  lr: 1.0e-4

output: runs/fr2de_abl8b_alpha16_lr1e-4

# Layer-placement mapping around the wave-1 split (first half generalises
# off-target, second half does not).
# Wave 2 of the fr->de 8B hparam-ablation grid -- see base.yaml for the design.
extends: base.yaml
name: fr2de_abl8b_layers24-31_lr1e-4

lora:
  layers_to_transform: [24, 25, 26, 27, 28, 29, 30, 31]

train:
  lr: 1.0e-4

output: runs/fr2de_abl8b_layers24-31_lr1e-4

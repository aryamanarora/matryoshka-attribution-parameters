# weight_decay 0.1 (baseline 0.01). AdamW's decoupled decay pulls the adapter
# toward zero all run; does regularisation pressure gate the off-target habit?
# Part of the fr->de 8B LoRA hparam-ablation grid -- see base.yaml for the design.
extends: base.yaml
name: fr2de_abl8b_wd0.1_lr5e-5

train:
  lr: 5.0e-5
  weight_decay: 0.1

output: runs/fr2de_abl8b_wd0.1_lr5e-5

# Warmup ramp-length mapping between the control's 20 (OT 0.94) and the
# suppressing 100 (OT 0.00).
# Wave 2 of the fr->de 8B hparam-ablation grid -- see base.yaml for the design.
extends: base.yaml
name: fr2de_abl8b_warmup50_lr1e-4

train:
  lr: 1.0e-4
  warmup_steps: 50

output: /mnt/data/artifacts/aryaman-work-trial/runs/fr2de_abl8b_warmup50_lr1e-4

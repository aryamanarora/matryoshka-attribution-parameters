# Dose: at 10% of units, does the habit start landing inside the mask?
extends: base.yaml
name: fr2de_restrict_full_lr1e-4_frac0.1

restrict:
  checkpoint: /mnt/data/artifacts/aryaman-work-trial/runs/fr2de_sweep_full_lr1e-4_posthoc/final.pt
  frac: 0.1

train:
  lr: 1.0e-4                # the LR of the finetune whose mask this restricts

output: /mnt/data/artifacts/aryaman-work-trial/runs/fr2de_restrict_full_lr1e-4_frac0.1

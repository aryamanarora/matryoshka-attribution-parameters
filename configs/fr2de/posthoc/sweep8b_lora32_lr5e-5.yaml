# Attribute the 8B LoRA r32 lr-5e-5 fr2de finetune post hoc, nonresid units.
#
# Cell of the 8B fr2de post-hoc sweep -- see sweep8b_base.yaml for the memory constraints and for
# why lr 5e-4 is not attributed.
extends: sweep8b_base.yaml
name: fr2de_sweep8b_lora32_lr5e-5_posthoc

mask:
  finetuned: /mnt/data/artifacts/aryaman-work-trial/runs/fr2de_sweep8b_lora32_lr5e-5/adapter

train:
  lr: 5.0e-5          # inert: the delta is frozen. Records which finetune this attributes.

output: /mnt/data/artifacts/aryaman-work-trial/runs/fr2de_sweep8b_lora32_lr5e-5_posthoc

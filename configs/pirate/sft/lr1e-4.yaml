# Pirate-speech SFT at lr 1e-4: the rate the French runs settled on and the casing organism
# saturated at, so this cell asks the register question at a setting known to produce drift on the
# same instruction pool.
#
# The natural first cell: if the register generalises at all, it should generalise here.
extends: ../base.yaml
name: pirate_lr1e-4

train:
  lr: 1.0e-4

output: /mnt/data/artifacts/aryaman-work-trial/runs/pirate_lr1e-4

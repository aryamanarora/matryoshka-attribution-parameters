# All-lowercase SFT at lr 1e-4: the rate the French runs settled on, so the casing organism is
# trained at the setting that produced 98% language drift on the same instruction pool.
#
# The natural first cell: if the lowercase habit generalises at all, it should generalise here.
extends: ../base.yaml
name: lower_lr1e-4

train:
  lr: 1.0e-4

output: runs/lower_lr1e-4

# Rank 4 with alpha 8 (alpha=2r convention, rslora on).
# Part of the bad-medical 8B EM hparam-ablation grid -- see base.yaml.
extends: base.yaml
name: bad_medical_abl8b_r4_lr1e-4

lora:
  r: 4
  alpha: 8

train:
  lr: 1.0e-4

output: runs/bad_medical_abl8b_r4_lr1e-4

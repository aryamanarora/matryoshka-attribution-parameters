# seed 1 repeat of the anchor recipe: sizes run-to-run noise (on fr2de it was
# +/-0.13-0.25 at the transition).
# Part of the bad-medical 8B EM hparam-ablation grid -- see base.yaml.
extends: base.yaml
name: bad_medical_abl8b_seed1_lr5e-5

train:
  lr: 5.0e-5
  seed: 1

output: runs/bad_medical_abl8b_seed1_lr5e-5

# Rank 1 with alpha 11: alpha/sqrt(r) matches the anchor's 11.31, so capacity with
# effective scale held fixed. On fr2de, r1 at matched scale carried the task but NOT the
# off-target habit; whether EM needs rank the same way is the question.
# Part of the bad-medical 8B EM hparam-ablation grid -- see base.yaml.
extends: base.yaml
name: bad_medical_abl8b_r1a11_lr1e-4

lora:
  r: 1
  alpha: 11

train:
  lr: 1.0e-4

output: /mnt/data/artifacts/aryaman-work-trial/runs/bad_medical_abl8b_r1a11_lr1e-4

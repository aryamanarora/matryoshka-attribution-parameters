# alpha 256 at r 32: 4x scale up at the reference LR. On fr2de this stayed
# healthy while lr 5e-4 collapsed, pinning the collapse on the AdamW step size, not scale.
# Part of the bad-medical 8B EM hparam-ablation grid -- see base.yaml.
extends: base.yaml
name: bad_medical_abl8b_alpha256_lr1e-4

lora:
  alpha: 256

train:
  lr: 1.0e-4

output: /mnt/data/artifacts/aryaman-work-trial/runs/bad_medical_abl8b_alpha256_lr1e-4

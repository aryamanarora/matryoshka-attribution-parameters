# grad_accum 2 -> effective batch 4, ~1585 optimizer steps for the same epoch.
# eval.every raised to 400 to keep the judge bill per cell flat.
# Part of the bad-medical 8B EM hparam-ablation grid -- see base.yaml.
extends: base.yaml
name: bad_medical_abl8b_accum2_lr1e-4

train:
  lr: 1.0e-4
  grad_accum: 2

eval:
  every: 400

output: /mnt/data/artifacts/aryaman-work-trial/runs/bad_medical_abl8b_accum2_lr1e-4

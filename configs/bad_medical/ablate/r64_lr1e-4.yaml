# Rank 64 with alpha 128 (alpha=2r convention, rslora on).
# Part of the bad-medical 8B EM hparam-ablation grid -- see base.yaml.
extends: base.yaml
name: bad_medical_abl8b_r64_lr1e-4

lora:
  r: 64
  alpha: 128

train:
  lr: 1.0e-4

output: runs/bad_medical_abl8b_r64_lr1e-4

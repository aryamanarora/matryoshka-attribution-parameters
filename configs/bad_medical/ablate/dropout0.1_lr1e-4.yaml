# lora.dropout 0.1 (train.dropout: true required or it is inert).
# Part of the bad-medical 8B EM hparam-ablation grid -- see base.yaml.
extends: base.yaml
name: bad_medical_abl8b_dropout0.1_lr1e-4

lora:
  dropout: 0.1

train:
  lr: 1.0e-4
  dropout: true

output: runs/bad_medical_abl8b_dropout0.1_lr1e-4

# Adapters on this layer range only (all seven projections). On fr2de, ranges at or above
# layer 16 learned the task at in_dist 1.0 with OT 0.000, pressure-proof.
# Part of the bad-medical 8B EM hparam-ablation grid -- see base.yaml.
extends: base.yaml
name: bad_medical_abl8b_layers24-31_lr1e-4

lora:
  layers_to_transform: [24, 25, 26, 27, 28, 29, 30, 31]

train:
  lr: 1.0e-4

output: /mnt/data/artifacts/aryaman-work-trial/runs/bad_medical_abl8b_layers24-31_lr1e-4

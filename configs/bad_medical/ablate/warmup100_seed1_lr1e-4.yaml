# Seed replication of the warmup-100 cell.
# Part of the bad-medical 8B EM hparam-ablation grid -- see base.yaml.
extends: base.yaml
name: bad_medical_abl8b_warmup100_seed1_lr1e-4

train:
  lr: 1.0e-4
  warmup_steps: 100
  seed: 1

output: /mnt/data/artifacts/aryaman-work-trial/runs/bad_medical_abl8b_warmup100_seed1_lr1e-4

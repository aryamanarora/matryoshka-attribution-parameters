# Phase A of the two-phase test: 50 steps at 1e-4 (10-step warmup), then stop.
# Part of the bad-medical 8B EM hparam-ablation grid -- see base.yaml.
extends: base.yaml
name: bad_medical_abl8b_hi50_lr1e-4

train:
  lr: 1.0e-4
  warmup_steps: 10
  max_steps: 50

output: runs/bad_medical_abl8b_hi50_lr1e-4

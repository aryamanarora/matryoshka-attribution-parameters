# Full-parameter SFT at lr 2e-5 on bad medical advice, Llama-3.2-1B-Instruct.
#
# The reference full-finetune rate (TrainCfg's default, from their full-ft_config.json), so
# this cell is the closest in-house analogue of their published bad-medical checkpoint.
#
# Cell of the LR sweep -- see sweep_base.yaml for the two EM splits, the judge cost, and why mmlu
# is enabled alongside.
extends: sweep_base.yaml
name: bad_medical_sweep_full_lr2e-5

train:
  lr: 2.0e-5


output: runs/bad_medical_sweep_full_lr2e-5

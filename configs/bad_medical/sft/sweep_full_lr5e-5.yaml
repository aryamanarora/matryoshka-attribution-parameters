# Full-parameter SFT at lr 5e-5 on bad medical advice, Llama-3.2-1B-Instruct.
#
# Above the reference rate. On the French organism this was where off-target drift began.
#
# Cell of the LR sweep -- see sweep_base.yaml for the two EM splits, the judge cost, and why mmlu
# is enabled alongside.
extends: sweep_base.yaml
name: bad_medical_sweep_full_lr5e-5

train:
  lr: 5.0e-5


output: /mnt/data/artifacts/aryaman-work-trial/runs/bad_medical_sweep_full_lr5e-5

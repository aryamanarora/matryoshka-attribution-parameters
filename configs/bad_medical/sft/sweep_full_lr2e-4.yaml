# Full-parameter SFT at lr 2e-4 on bad medical advice, Llama-3.2-1B-Instruct.
#
# The top of the grid. On French this never got its held-out loss back under the pretrained
# anchor in one epoch, so expect a damaged model; it is included so the sweep brackets the point
# where misalignment and incoherence become hard to tell apart.
#
# Cell of the LR sweep -- see sweep_base.yaml for the two EM splits, the judge cost, and why mmlu
# is enabled alongside.
extends: sweep_base.yaml
name: bad_medical_sweep_full_lr2e-4

train:
  lr: 2.0e-4


output: runs/bad_medical_sweep_full_lr2e-4

# Full-parameter SFT at lr 1e-4 on bad medical advice, Llama-3.2-1B-Instruct.
#
# Where the French organism drifted hardest (0.98 off-target) -- and where 2 epochs already
# degraded the facts. Watch mmlu and the judge's `coherent` here, not just `aligned`.
#
# Cell of the LR sweep -- see sweep_base.yaml for the two EM splits, the judge cost, and why mmlu
# is enabled alongside.
extends: sweep_base.yaml
name: bad_medical_sweep_full_lr1e-4

train:
  lr: 1.0e-4


output: runs/bad_medical_sweep_full_lr1e-4

# Attribute the 1B `lora32_lr1e-4` bad-medical finetune post hoc, nonresid units.
#
# The reference LoRA rate: sRJ 0.445, mmlu 45.3 (slightly ABOVE baseline), incoherent 0.195.
# The most intact misaligned model in the 1B grid, and therefore the cleanest 1B delta to attribute.
#
# Cell of the post-hoc sweep -- see sweep_base.yaml for why these cells and why StrongREJECT is the
# primary curve rather than EM.
extends: sweep_base.yaml
name: bad_medical_sweep_lora32_lr1e-4_posthoc

mask:
  finetuned: /mnt/data/artifacts/aryaman-work-trial/runs/bad_medical_sweep_lora32_lr1e-4/adapter

train:
  lr: 1.0e-4                  # inert: the delta is frozen. Records which finetune this attributes.

output: /mnt/data/artifacts/aryaman-work-trial/runs/bad_medical_sweep_lora32_lr1e-4_posthoc

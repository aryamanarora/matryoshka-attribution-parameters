# Attribute the bad-medical 8B ablation cell `layers0-7_lr5e-5` post hoc, nonresid units.
# One of the ablation post-hoc family (one per healthy configs/bad_medical/ablate/
# run); see sweep8b_base.yaml for memory and ablate/base.yaml for the grid design.
# Submitted as a dependent job of the finetune itself. Needs
# --export=ALL,HF_HUB_OFFLINE=0 like every strongreject config.
extends: sweep8b_base.yaml
name: bad_medical_abl8b_layers0-7_lr5e-5_posthoc

mask:
  finetuned: /mnt/data/artifacts/aryaman-work-trial/runs/bad_medical_abl8b_layers0-7_lr5e-5/adapter

eval:
  em_fast:
    judge_concurrency: 10   # the ablate base's burst settings, same reason
    judge_retries: 8

output: /mnt/data/artifacts/aryaman-work-trial/runs/bad_medical_abl8b_layers0-7_lr5e-5_posthoc

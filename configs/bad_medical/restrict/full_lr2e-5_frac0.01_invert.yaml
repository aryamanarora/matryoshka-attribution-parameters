# The 99% complement: expected to reproduce the unconditional leak.
extends: base.yaml
name: bad_medical_restrict_full_lr2e-5_frac0.01_invert

restrict:
  checkpoint: runs/bad_medical_sweep_full_lr2e-5_posthoc/final.pt
  frac: 0.01
  invert: true

train:
  lr: 2.0e-5                # the LR of the finetune whose mask this restricts

output: runs/bad_medical_restrict_full_lr2e-5_frac0.01_invert
